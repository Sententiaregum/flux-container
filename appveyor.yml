environment:
  nodejs_version: "5.8"

install:
  - npm install -g babel-cli eslint-plugin-varspacing eslint
  - npm install react
  - npm install

test_script:
  - node --version
  - npm --version
  - npm run lint
  - npm run test

init:
  - git config --global core.autocrlf true

build: false
